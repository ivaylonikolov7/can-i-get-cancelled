{
  "dependencies": {
    "axios": "^0.21.4",
    "bootstrap": "^5.1.1",
    "create-react-app": "^4.0.3",
    "express-generator": "^4.16.1",
    "react-bootstrap": "^2.0.0-rc.0"
  },
  "scripts": {
    "start": "cd server && npm install && node ./bin/www",
    "build": "cd frontend && npm install && yarn build"
  }
}
